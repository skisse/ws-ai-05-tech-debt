Review $ARGUMENTS against all architecture rules in CLAUDE.md and .claude/rules/.

For each violation found:
1. State the exact line number
2. State which rule is violated and its severity (CRITICAL / HIGH / MEDIUM / LOW)
3. Explain why it is a problem
4. Show the corrected code

Group findings by severity, highest first. If no violations: confirm the file is compliant and state which rules were checked.
